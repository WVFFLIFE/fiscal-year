import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles(() => ({
  root: {
    margin: 0,
  },
  label: {
    marginLeft: 15,
    fontSize: 14,
    fontFamily: 'Proxima Nova',
    lineHeight: 1,
    color: '#000',
  },
}));
