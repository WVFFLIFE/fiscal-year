import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles(() => ({
  btnsWrapper: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'flex-end',
    marginTop: 30,
  },
}));
