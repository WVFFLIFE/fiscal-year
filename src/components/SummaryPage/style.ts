import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles(() => ({
  offsetTop: {
    marginTop: 10,
  },
  offsetBottom: {
    marginBottom: 20,
  },
  filterItem: {
    marginRight: 0,
  },
}));
