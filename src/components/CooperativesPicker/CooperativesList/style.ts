import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles((theme) => ({
  menuList: {
    maxHeight: 205,
    marginBottom: 20,
    padding: 0,
    overflow: 'auto',
    overflowX: 'hidden',
  },
}));
