import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles(() => ({
  h1: {
    margin: 0,
    padding: 0,
    fontSize: 24,
    fontFamily: 'Proxima Nova',
    fontWeight: 600,
    color: '#000',
  },
}));
