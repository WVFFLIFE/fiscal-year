import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles(() => ({
  btn: {
    position: 'absolute',
    top: 20,
    right: 0,

    fontSize: 14,
    fontFamily: 'Proxima Nova',
    lineHeight: '16px',
  },
}));
