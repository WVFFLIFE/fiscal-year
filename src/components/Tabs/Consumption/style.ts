import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles(() => ({
  subtitle: {
    marginBottom: 20,
  },
  dropzone: {
    maxWidth: 500,
    flex: 1,
  },
  preview: {
    marginLeft: 20,
  },
}));
