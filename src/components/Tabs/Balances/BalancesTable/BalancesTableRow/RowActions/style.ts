import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles(() => ({
  root: {
    display: 'flex',
    alignItems: 'center',
  },
  btnOffset: {
    marginRight: 10,
  },
  icon: {
    fontSize: 12,
  },
}));
