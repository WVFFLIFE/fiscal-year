import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles(() => ({
  root: {
    display: 'flex',
    alignItems: 'center',
  },
  btn: {
    marginLeft: 10,
  },
  searchIcon: {
    fontSize: 12,
  },
}));
