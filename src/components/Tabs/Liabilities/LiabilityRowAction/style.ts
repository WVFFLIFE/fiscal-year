import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles(() => ({
  root: {
    display: 'flex',
    alignItems: 'center',
  },
  btnOffset: {
    marginLeft: 10,
  },
  icon: {
    fontSize: 16,
  },
}));
